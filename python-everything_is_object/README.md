## What is an object  
    Something that a vairable can refer to.

## What is the difference between a class and an object or instance
## What is the difference between immutable object and mutable object  
    Immutbale object cannot be addition or modified by another variable. On the opposite, mutable object can be.
## What is a reference
## What is an assignment
## What is an alias  
    Alias is used to assign a vairbale to another variable (a = b). If one variable is modified, the other will be modified too.
## How to know if two variables are identical  
    We can use "==" (a == b)
## How to know if two variables are linked to the same object  
    We can use "is" (a is b)
## How to display the variable identifier (which is the memory address in the CPython implementation)  
    Using "id" (id(a))
## What is mutable and immutable
## What are the built-in mutable types
## What are the built-in immutable types
## How does Python pass variables to functions